import official/tokakv/cache/block_cache::{ValueLease}

fn escape_view() -> str {
    auto owned = string::from("owned")
    auto lease = ValueLease::from_owned(cede owned)
    return lease.as_str()
}

fn main() -> i32 {
    return escape_view().len() as i32
}
